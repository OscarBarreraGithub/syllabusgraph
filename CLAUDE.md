@AGENTS.md

## Claude Code

Use the project agents in `.claude/agents/` only when the active course policy
selects Claude. The orchestrator is the only role that may delegate. Review
the actual model shown by Claude Code before completing a dispatch; if it was
substituted or cannot meet the recorded request, do not complete it as the
requested run.
